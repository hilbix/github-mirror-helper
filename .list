#!/usr/bin/env python3

import sys
import git
import github

c = git.config.GitConfigParser([git.Repo._get_config_path(None, f) for f in ['system','user','global']], read_only=True)
print(c)

sys.exit(1)

git.config.read()
g = github.Github("access_token")
